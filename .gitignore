node_modules/
dist/
.env
.env.local
.DS_Store
# Local Netlify folder
.netlify

# Deno lockfile generated locally by `netlify dev` edge bundling; Netlify
# regenerates its own during deploy, so this is a local-only artifact.
deno.lock

# Local Claude Code tooling
.claude/
