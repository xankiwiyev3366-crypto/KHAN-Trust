# Whitepaper PDF

Place the final PDF here as `khan-ecosystem-whitepaper.pdf`.

To ship a new version in the future, replace this file and update
`src/whitepaperConfig.js` (version, release date, page count). No other
code needs to change.
